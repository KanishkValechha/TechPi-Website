---
// Hero section with infinitely moving background and text overlay
---

<section class="relative bg-primary min-h-screen text-white overflow-hidden">
    <div class="absolute inset-0 moving-background"></div>
    <div class="absolute inset-0 bg-black opacity-70"></div>
    
    <div class="relative container mx-auto px-4 py-12 md:py-32 flex items-center justify-center overflow-hidden brightness-100 text-center">
        <div class="grid grid-cols-1 place-items-center gap-12 items-center mt-22 md:mt-18 max-w-3xl mx-auto">
            <div 
                class="text-content-overlay p-8 rounded-xl bg-black/40 backdrop-blur-sm"
                data-aos="fade-right"
            >
                <h2 class="text-5xl font-bold mt-2 mb-2 text-white leading-tight">
                    <span class="text-accent/90">TechPI Solutions</span>
                </h2>
                <div class="flex justify-center">
                    <div class="w-36 md:w-96 h-1 bg-accent/90 rounded-full mb-4"></div>
                </div>
                <h1 class="text-4xl md:text-6xl font-bold mb-6 leading-tight">
                    Excellence in Electrical Infrastructure
                </h1>
                <p class="text-xl mb-8 text-gray-100">
                    Leading manufacturer of Cable Trays and Low Voltage Panels, delivering innovative solutions with uncompromising quality and reliability.
                </p>
                <div class="flex flex-wrap gap-4 justify-center">
                    <a 
                        href="/products" 
                        class="bg-secondary hover:bg-opacity-90 px-8 py-4 rounded-lg font-semibold text-lg transition-all transform hover:scale-105"
                    >
                        Explore Products
                    </a>
                    <a 
                        href="/contact" 
                        class="bg-white text-primary hover:bg-gray-100 px-8 py-4 rounded-lg font-semibold text-lg transition-all transform hover:scale-105"
                    >
                        Get in Touch
                    </a>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .moving-background {
        background-image: url('../images/collage.jpg');
        background-size: auto 100%;
        background-repeat: repeat-x;
        animation: move-background 20s linear infinite;
    }

    @keyframes move-background {
        0% {
            background-position: 0 0;
        }
        100% {
            background-position: -2000px 0;
        }
    }

    .text-content-overlay {
        box-shadow: 0 10px 30px rgba(0,0,0,0.3);
    }
</style>